import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    fontSize: 20,
    color: "red", //"#000",
  },
  des: {
    fontSize: 18,
    color: "#000",
  },
  btnContainer: {
    position: "absolute",
    bottom: 24,
    right: 24,
  },
});

export default styles;

import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    fontSize: 20,
    color: 'red',//"#000",
  },
  des:{
    fontSize:18,
    color: "#000",
  }
});

export default styles;
